const ProfilePicture = artifacts.require("./ProfilePicture.sol");

contract("ProfilePicture", accounts => {
  it("should ...", async () => {
    // const profilePictureInstance = await ProfilePicture.deployed();

    // // Set value of 89
    // await profilePictureInstance.set(89, { from: accounts[0] });

    // // Get stored value
    // const storedData = await profilePictureInstance.get.call();

    // assert.equal(storedData, 89, "The value 89 was not stored.");
  });
});
